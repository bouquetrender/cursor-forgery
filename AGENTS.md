# AGENTS.md

本文件适用于在本仓库中修改代码的智能体。开始修改前必须先阅读，并以仓库现有实现为准。

## 项目概览

`cursor-forgery` 是一个 VS Code 扩展，使用 TypeScript、CommonJS 和 VS Code 1.85 API。
扩展用于为外部编程智能体产生的文本文件改动提供基线、差异审查和接受/拒绝操作；不调用 AI API。

### 目录职责

- `src/extension.ts`：扩展入口、依赖装配和命令注册。
- `src/session/`：审查会话与基线存储。`BaselineStore` 是不同存储实现的抽象边界。
- `src/diff/`：基线与当前文档的差异计算及缓存。
- `src/commands/`：命令处理，仅协调会话、基线和差异服务。
- `src/ui/`：Tree View、CodeLens、状态栏及基线文本内容提供者。
- `src/test/unit/`：不依赖 VS Code 扩展宿主的单元测试。
- `src/test/suite/`：在 VS Code 扩展宿主中运行的集成测试。
- `package.json`：扩展清单、贡献点、命令和 npm 脚本。

## 修改前

1. 阅读目标文件及其直接调用方，确认数据流和已有约定。
2. 先检查工作区状态；保留与本次任务无关的已有改动，不覆盖或格式化它们。
3. 若需求存在多种合理解释、会改变用户可见行为或涉及接受/拒绝语义，先说明假设并向用户确认。

## 实现原则

- 使用 TypeScript 严格类型；不以 `any`、类型断言或静默吞错绕过类型问题。
- 保持最小改动：不重构无关代码，不新增需求之外的抽象、配置或功能。
- 延续现有风格：双引号、分号、尾随逗号，以及 `import type` 的类型导入写法。
- 值得保留的注释只解释非显而易见的原因、约束或失效条件，不复述代码行为。
- 新增 VS Code 命令时，同时更新 `package.json` 的 `contributes.commands`；如需出现在菜单或视图，也补齐对应贡献点。
- 新增可释放的 VS Code 资源（订阅、提供者、Tree View 等）必须加入 `context.subscriptions`。
- 变更基线、差异或审查状态后，确保依赖该状态的 UI 能收到现有事件机制的刷新通知。

## 领域约束

- 基线代表审查开始时的文件内容；接受变更只推进对应基线，拒绝变更通过 `WorkspaceEdit` 恢复基线内容。
- 编辑器中的未保存修改属于用户编辑；直接文件系统修改属于外部智能体修改。这一区分不能被破坏。
- 已有 UTF-8 文本文件的修改进入待审查范围；新增、删除文件仅作为不可接受或拒绝的整文件历史记录。重命名不单独识别，二进制和非 UTF-8 文件不在范围内。
- Git 工作区必须继续使用隔离的临时索引、对象目录和工作树，绝不能改动真实 Git 索引或暂存区。
- 非 Git 与多根工作区通过 `BaselineStore` 回退到内存基线；新增存储实现时保持该接口兼容。

## 验证与交付

1. 通过阅读调用路径确认改动不会破坏既有状态流。
2. 仅在用户明确要求时运行验证命令；可用命令为 `npm run compile`、`npm run test:unit` 和 `npm run test:integration`。
3. 交付时说明修改的文件、未运行的验证及其原因，以及仍需用户决定的事项。

## 避免事项

- 不修改 `out/` 生成物，也不提交构建产物。
- 不使用破坏性 Git 命令（如 `git reset --hard`、`git checkout --`）处理已有改动。
- 不扩大当前审查范围，不将 AI 调用、遥测或网络请求引入扩展。
